# Location Voiture - نسخة المبتدئين المبسّطة

## ✅ تم الانتهاء من التبسيط!

المشروع تم تحويله من **معقد (56 ملف)** إلى **بسيط (15 ملف)**

---

## 🚀 للبدء الآن:

### الخادم يعمل على:
```
http://127.0.0.1:8000/
```

### حساب المسؤول:
```
Username: admin
Password: password123
```

---

## 📝 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `cars/models.py` | نموذج السيارة والحجز |
| `cars/views.py` | 6 دوال بسيطة فقط |
| `accounts/forms.py` | نموذج التسجيل والدخول |
| `templates/base.html` | القالب الأساسي |
| `README_BEGINNER.md` | دليل للمبتدئين |
| `COMPLETE_SUMMARY.md` | ملخص كامل |

---

## 🎯 ما تعلمته:

### 1️⃣ Models البسيطة
```python
class Car(models.Model):
    name = CharField()
    price_per_day = IntegerField()
    description = TextField()
    available = BooleanField()

class Booking(models.Model):
    customer_name = CharField()
    car = ForeignKey(Car)
    start_date = DateField()
    end_date = DateField()
    total_price = IntegerField()
```

### 2️⃣ Views البسيطة
```python
def home(request):
    cars = Car.objects.all()[:6]
    return render(request, 'cars/home.html', {'cars': cars})

def book_car(request, pk):
    # ... منطق الحجز البسيط ...
    pass
```

### 3️⃣ URLs البسيطة
```python
path('', views.car_list, name='car_list')
path('<int:pk>/', views.car_detail, name='car_detail')
path('<int:pk>/book/', views.book_car, name='book_car')
```

---

## 🔗 الروابط الرئيسية

- الرئيسية: `/`
- السيارات: `/cars/`
- تسجيل: `/accounts/signup/`
- دخول: `/accounts/login/`
- لوحة التحكم: `/admin/`

---

## 📊 الإحصائيات

| العنصر | قبل | بعد |
|--------|------|-----|
| عدد الملفات | 56 | 15 |
| عدد السطور (models) | 200+ | 28 |
| عدد السطور (views) | 197 | 65 |
| المكتبات الإضافية | 7 | 2 |
| Complexity | ⭐⭐⭐⭐⭐ | ⭐⭐ |

---

## 🎓 للتعلم أكثر:

1. قراءة `models.py` - فهم البيانات
2. قراءة `views.py` - فهم المنطق
3. قراءة `templates/` - فهم الواجهة
4. تعديل الأكواد - التعلم بالتطبيق
5. إضافة ميزات جديدة - التطور

---

## 🎉 الخطوات التالية:

- [ ] اقرأ `README_BEGINNER.md`
- [ ] جرّب المشروع من المتصفح
- [ ] أضف 3 سيارات من `/admin`
- [ ] سجّل حساب جديد
- [ ] احجز سيارة
- [ ] اقرأ الأكواد وافهمها
- [ ] أضف ميزة جديدة (مثلاً: تقييمات)

---

**Made with ❤️ for beginners**

تاريخ التبسيط: 10/12/2025
